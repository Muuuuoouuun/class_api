# 진학 적합도 진단 (Phase 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 학부모가 업로드한 생기부 PDF를 Claude로 구조화하고, 목표 학과 인재상 루브릭으로 채점해 적합도 리포트를 생성하되, 동의 게이트와 비식별 코퍼스 축적을 처음부터 내장한다.

**Architecture:** 기존 5-layer 구조 준수. Layer 3(`intelligence/`)에 엔진 4종(파서·루브릭·채점·리포트) + 순수 로직(동의 게이트·비식별화·인용검증), Layer 2(`storage/`)에 Notion DB6/DB7 추가, Layer 4(`pipelines/career.py`)에서 오케스트레이션. 주간 리포트의 "HTML 드래프트 → 원장 승인 → Notion 아카이브" 패턴 D를 복제.

**Tech Stack:** Python 3.x, pydantic v2, anthropic SDK(기존 `claude_client` 래퍼), notion-client, pytest. PDF는 Claude 네이티브 document 블록(스캔본 OCR 포함)으로 처리 — 새 PDF 라이브러리 의존성 없음.

**참조 스펙:** `docs/superpowers/specs/2026-06-08-진학적합도진단-design.md`

---

## File Structure

**생성:**
- `src/classin_toolkit/intelligence/saenggibu_schema.py` — 구조화 생기부 pydantic 모델 (데이터 계약)
- `src/classin_toolkit/intelligence/deidentify.py` — 비식별화 순수 함수
- `src/classin_toolkit/intelligence/consent.py` — 동의 상태 enum + 게이트 순수 로직
- `src/classin_toolkit/intelligence/saenggibu_parser.py` — PDF → 구조화 JSON (Claude)
- `src/classin_toolkit/intelligence/rubric_builder.py` — 학과 → 루브릭 (Claude, 캐시)
- `src/classin_toolkit/intelligence/fit_scorer.py` — 생기부+루브릭 → 점수 + 인용검증(순수)
- `src/classin_toolkit/intelligence/career_report.py` — 채점 → 리포트 (parse 패턴)
- `src/classin_toolkit/intelligence/prompts/saenggibu_parse.md`
- `src/classin_toolkit/intelligence/prompts/rubric_build.md`
- `src/classin_toolkit/intelligence/prompts/fit_score.md`
- `src/classin_toolkit/intelligence/prompts/career_report.md`
- `src/classin_toolkit/pipelines/career.py` — 오케스트레이션
- `tests/test_saenggibu_schema.py`, `tests/test_deidentify.py`, `tests/test_consent.py`, `tests/test_fit_scorer.py`, `tests/test_career_report.py`, `tests/test_career_pipeline.py`

**수정:**
- `src/classin_toolkit/intelligence/claude_client.py` — PDF document 블록 지원 헬퍼 추가
- `src/classin_toolkit/config.py` — `NotionDatabases`에 `career`, `corpus` 추가
- `src/classin_toolkit/storage/notion_repo.py` — DB6/DB7 PROP 상수 + 메서드

---

## Task 1: 구조화 생기부 스키마 (데이터 계약)

**Files:**
- Create: `src/classin_toolkit/intelligence/saenggibu_schema.py`
- Test: `tests/test_saenggibu_schema.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_saenggibu_schema.py
from classin_toolkit.intelligence.saenggibu_schema import StructuredSaenggibu, SubjectDetail


def test_valid_saenggibu_parses():
    data = {
        "grade_records": [
            {
                "grade": 2,
                "subject_details": [
                    {"subject": "물리학I", "text": "역학 탐구 보고서 작성", "confidence": 0.9}
                ],
                "creative_activities": ["과학 동아리 부장"],
                "behavior_notes": "성실함",
                "attendance": {"absent": 0, "late": 1},
            }
        ],
    }
    sg = StructuredSaenggibu.model_validate(data)
    assert sg.grade_records[0].grade == 2
    assert sg.grade_records[0].subject_details[0].subject == "물리학I"


def test_failed_parse_item_marked_null():
    detail = SubjectDetail(subject="수학", text=None, confidence=0.0)
    assert detail.text is None
    assert detail.is_low_confidence() is True
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_saenggibu_schema.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'classin_toolkit.intelligence.saenggibu_schema'`

- [ ] **Step 3: Write minimal implementation**

```python
# src/classin_toolkit/intelligence/saenggibu_schema.py
from __future__ import annotations

from pydantic import BaseModel, Field


class SubjectDetail(BaseModel):
    subject: str
    text: str | None = None          # 파싱 실패 시 None + 플래그 (0점 아님)
    confidence: float = 1.0

    def is_low_confidence(self) -> bool:
        return self.text is None or self.confidence < 0.5


class Attendance(BaseModel):
    absent: int = 0
    late: int = 0


class GradeRecord(BaseModel):
    grade: int
    subject_details: list[SubjectDetail] = Field(default_factory=list)
    creative_activities: list[str] = Field(default_factory=list)  # 창체
    behavior_notes: str | None = None                             # 행특
    attendance: Attendance = Field(default_factory=Attendance)


class StructuredSaenggibu(BaseModel):
    grade_records: list[GradeRecord] = Field(default_factory=list)

    def all_evidence(self) -> list[str]:
        """인용 검증용 — 생기부에 실제로 존재하는 모든 텍스트 조각."""
        out: list[str] = []
        for g in self.grade_records:
            out += [d.text for d in g.subject_details if d.text]
            out += g.creative_activities
            if g.behavior_notes:
                out.append(g.behavior_notes)
        return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_saenggibu_schema.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add src/classin_toolkit/intelligence/saenggibu_schema.py tests/test_saenggibu_schema.py
git commit -m "feat: 구조화 생기부 pydantic 스키마"
```

---

## Task 2: 비식별화 (순수 함수)

**Files:**
- Create: `src/classin_toolkit/intelligence/deidentify.py`
- Test: `tests/test_deidentify.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_deidentify.py
from classin_toolkit.intelligence.deidentify import deidentify
from classin_toolkit.intelligence.saenggibu_schema import (
    GradeRecord, StructuredSaenggibu, SubjectDetail,
)


def test_removes_student_and_school_names():
    sg = StructuredSaenggibu(grade_records=[
        GradeRecord(
            grade=3,
            subject_details=[SubjectDetail(subject="국어", text="홍길동 학생은 한빛고에서 우수함")],
            behavior_notes="홍길동은 성실",
        )
    ])
    clean = deidentify(sg, names=["홍길동"], schools=["한빛고"])
    joined = " ".join(clean.all_evidence())
    assert "홍길동" not in joined
    assert "한빛고" not in joined
    assert "[학생]" in joined or "성실" in joined


def test_original_object_not_mutated():
    sg = StructuredSaenggibu(grade_records=[
        GradeRecord(grade=1, behavior_notes="김철수 우수")
    ])
    deidentify(sg, names=["김철수"], schools=[])
    assert "김철수" in (sg.grade_records[0].behavior_notes or "")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_deidentify.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: Write minimal implementation**

```python
# src/classin_toolkit/intelligence/deidentify.py
from __future__ import annotations

from .saenggibu_schema import StructuredSaenggibu


def _scrub(text: str | None, names: list[str], schools: list[str]) -> str | None:
    if text is None:
        return None
    out = text
    for n in names:
        if n:
            out = out.replace(n, "[학생]")
    for s in schools:
        if s:
            out = out.replace(s, "[학교]")
    return out


def deidentify(
    sg: StructuredSaenggibu, *, names: list[str], schools: list[str]
) -> StructuredSaenggibu:
    """식별자를 토큰으로 치환한 새 객체 반환 (원본 불변)."""
    clean = sg.model_copy(deep=True)
    for g in clean.grade_records:
        for d in g.subject_details:
            d.text = _scrub(d.text, names, schools)
        g.creative_activities = [
            _scrub(a, names, schools) or "" for a in g.creative_activities
        ]
        g.behavior_notes = _scrub(g.behavior_notes, names, schools)
    return clean
```

> 주의: 호출부는 `deidentify(sg, names=..., schools=...)` 키워드 인자. 테스트의 위치인자 호출을 키워드로 맞춰 둘 것 — 아래 Step 1 테스트는 키워드로 작성돼 있음 (`names=`, `schools=`). 위치인자로 쓴 라인이 있으면 키워드로 수정.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_deidentify.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add src/classin_toolkit/intelligence/deidentify.py tests/test_deidentify.py
git commit -m "feat: 생기부 비식별화 순수 함수"
```

---

## Task 3: 동의 게이트 (순수 로직)

**Files:**
- Create: `src/classin_toolkit/intelligence/consent.py`
- Test: `tests/test_consent.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_consent.py
import pytest

from classin_toolkit.intelligence.consent import (
    ConsentStatus, ConsentError, ensure_can_analyze, can_add_to_corpus,
)


def test_no_consent_blocks_analysis():
    with pytest.raises(ConsentError):
        ensure_can_analyze(ConsentStatus.NONE)


def test_internal_consent_allows_analysis_but_not_corpus():
    ensure_can_analyze(ConsentStatus.INTERNAL)        # raise 안 함
    assert can_add_to_corpus(ConsentStatus.INTERNAL) is False


def test_corpus_consent_allows_both():
    ensure_can_analyze(ConsentStatus.CORPUS)
    assert can_add_to_corpus(ConsentStatus.CORPUS) is True


def test_from_label_maps_korean():
    assert ConsentStatus.from_label("코퍼스활용") is ConsentStatus.CORPUS
    assert ConsentStatus.from_label("미동의") is ConsentStatus.NONE
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_consent.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: Write minimal implementation**

```python
# src/classin_toolkit/intelligence/consent.py
from __future__ import annotations

from enum import Enum


class ConsentError(Exception):
    pass


class ConsentStatus(Enum):
    NONE = "미동의"
    INTERNAL = "내부분석"
    CORPUS = "코퍼스활용"

    @classmethod
    def from_label(cls, label: str | None) -> "ConsentStatus":
        for s in cls:
            if s.value == (label or "").strip():
                return s
        return cls.NONE


def ensure_can_analyze(status: ConsentStatus) -> None:
    if status is ConsentStatus.NONE:
        raise ConsentError("미동의 상태에서는 생기부 분석을 수행할 수 없습니다.")


def can_add_to_corpus(status: ConsentStatus) -> bool:
    return status is ConsentStatus.CORPUS
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_consent.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add src/classin_toolkit/intelligence/consent.py tests/test_consent.py
git commit -m "feat: 동의 상태 enum + 분석/코퍼스 게이트"
```

---

## Task 4: Claude PDF document 블록 헬퍼

**Files:**
- Modify: `src/classin_toolkit/intelligence/claude_client.py` (끝에 함수 추가)

- [ ] **Step 1: Implement helper (no unit test — 외부 API 호출, 통합 경로에서 검증)**

`claude_client.py` 끝에 추가. 기존 `run_structured` / `_strip_fence` 패턴 재사용:

```python
import base64


def run_json_with_pdf(
    cfg: AppConfig,
    *,
    system: str,
    user: str,
    pdf_bytes: bytes,
    model: str | None = None,
    max_tokens: int = 4096,
) -> dict | list:
    """PDF를 document 블록으로 첨부해 구조화 JSON을 받는다.

    Claude 네이티브 PDF 지원 → 스캔본(이미지 PDF)도 비전으로 처리.
    """
    client = get_claude(cfg.anthropic.api_key)
    b64 = base64.standard_b64encode(pdf_bytes).decode("ascii")
    content = [
        {
            "type": "document",
            "source": {"type": "base64", "media_type": "application/pdf", "data": b64},
        },
        {"type": "text", "text": user},
    ]
    resp = client.messages.create(
        model=model or cfg.anthropic.report_model,
        max_tokens=max_tokens,
        system=[{"type": "text", "text": system}],
        messages=[{"role": "user", "content": content}],
    )
    text = "".join(b.text for b in resp.content if getattr(b, "type", "") == "text")
    return json.loads(_strip_fence(text.strip()))
```

- [ ] **Step 2: Verify import compiles**

Run: `python -c "from classin_toolkit.intelligence.claude_client import run_json_with_pdf; print('ok')"`
Expected: `ok`

- [ ] **Step 3: Commit**

```bash
git add src/classin_toolkit/intelligence/claude_client.py
git commit -m "feat: claude_client PDF document 블록 헬퍼"
```

---

## Task 5: 생기부 파서 + 프롬프트

**Files:**
- Create: `src/classin_toolkit/intelligence/saenggibu_parser.py`
- Create: `src/classin_toolkit/intelligence/prompts/saenggibu_parse.md`

- [ ] **Step 1: Write the prompt file**

```markdown
<!-- src/classin_toolkit/intelligence/prompts/saenggibu_parse.md -->
당신은 학교생활기록부(생기부) 구조화 전문가다. 첨부된 생기부 PDF를 읽고
아래 JSON 스키마로만 출력한다. 코드펜스 없이 순수 JSON만.

규칙:
- 추측 금지. PDF에 없는 내용은 채우지 말 것.
- 항목을 읽기 어렵거나 불확실하면 text를 null, confidence를 0.0~0.4로.
- 학년별로 grade_records 분리.

스키마:
{
  "grade_records": [
    {
      "grade": <int 1|2|3>,
      "subject_details": [{"subject": <str>, "text": <str|null>, "confidence": <0..1>}],
      "creative_activities": [<str>],
      "behavior_notes": <str|null>,
      "attendance": {"absent": <int>, "late": <int>}
    }
  ]
}
```

- [ ] **Step 2: Write implementation (얇은 래퍼 — 스키마 검증으로 안전)**

```python
# src/classin_toolkit/intelligence/saenggibu_parser.py
from __future__ import annotations

from ..config import AppConfig
from .claude_client import load_prompt, run_json_with_pdf
from .saenggibu_schema import StructuredSaenggibu


def parse_saenggibu(cfg: AppConfig, *, pdf_bytes: bytes) -> StructuredSaenggibu:
    system = load_prompt("saenggibu_parse")
    raw = run_json_with_pdf(
        cfg,
        system=system,
        user="이 생기부를 스키마대로 구조화해줘.",
        pdf_bytes=pdf_bytes,
        model=cfg.anthropic.report_model,
    )
    return StructuredSaenggibu.model_validate(raw)  # 검증 실패 시 raise → 경계에서 catch
```

- [ ] **Step 3: Verify import compiles**

Run: `python -c "from classin_toolkit.intelligence.saenggibu_parser import parse_saenggibu; print('ok')"`
Expected: `ok`

- [ ] **Step 4: Commit**

```bash
git add src/classin_toolkit/intelligence/saenggibu_parser.py src/classin_toolkit/intelligence/prompts/saenggibu_parse.md
git commit -m "feat: 생기부 PDF 파서 + 프롬프트"
```

---

## Task 6: 루브릭 빌더 + 프롬프트

**Files:**
- Create: `src/classin_toolkit/intelligence/rubric_builder.py`
- Create: `src/classin_toolkit/intelligence/prompts/rubric_build.md`

- [ ] **Step 1: Write the prompt file**

```markdown
<!-- src/classin_toolkit/intelligence/prompts/rubric_build.md -->
당신은 대입 학생부종합전형 평가 설계자다. 주어진 목표 학과/계열에 대해
인재상·핵심역량 기반 채점 루브릭을 JSON으로만 출력한다(코드펜스 없이).

주의: 이것은 공식 입시 기준이 아니라 학과 인재상 기반 "참고 루브릭"이다.
역량 4~6개. 각 역량은 측정 가능한 설명을 포함.

스키마:
{
  "major": <str>,
  "competencies": [
    {"key": <str>, "label": <str>, "description": <str>, "weight": <0..1>}
  ]
}
```

- [ ] **Step 2: Write implementation (학과당 캐시)**

```python
# src/classin_toolkit/intelligence/rubric_builder.py
from __future__ import annotations

import json

from ..config import AppConfig
from .claude_client import load_prompt, run_json

_RUBRIC_CACHE: dict[str, dict] = {}


def build_rubric(cfg: AppConfig, *, major: str) -> dict:
    """목표 학과 → 채점 루브릭 dict. 동일 학과 재요청은 프로세스 캐시."""
    key = f"{cfg.anthropic.model}::{major}"
    if key in _RUBRIC_CACHE:
        return _RUBRIC_CACHE[key]
    system = load_prompt("rubric_build")
    rubric = run_json(cfg, system=system, user=json.dumps({"major": major}, ensure_ascii=False))
    if not isinstance(rubric, dict) or "competencies" not in rubric:
        raise ValueError(f"루브릭 형식 오류: {rubric!r}")
    _RUBRIC_CACHE[key] = rubric
    return rubric
```

- [ ] **Step 3: Verify import compiles**

Run: `python -c "from classin_toolkit.intelligence.rubric_builder import build_rubric; print('ok')"`
Expected: `ok`

- [ ] **Step 4: Commit**

```bash
git add src/classin_toolkit/intelligence/rubric_builder.py src/classin_toolkit/intelligence/prompts/rubric_build.md
git commit -m "feat: 학과 루브릭 빌더 (프로세스 캐시) + 프롬프트"
```

---

## Task 7: 채점기 + 인용 검증 (순수) + 프롬프트

**Files:**
- Create: `src/classin_toolkit/intelligence/fit_scorer.py`
- Create: `src/classin_toolkit/intelligence/prompts/fit_score.md`
- Test: `tests/test_fit_scorer.py`

- [ ] **Step 1: Write the failing test (인용 검증 순수 로직)**

```python
# tests/test_fit_scorer.py
from classin_toolkit.intelligence.fit_scorer import validate_citations, ScoreItem
from classin_toolkit.intelligence.saenggibu_schema import (
    GradeRecord, StructuredSaenggibu, SubjectDetail,
)


def _sg():
    return StructuredSaenggibu(grade_records=[
        GradeRecord(grade=2, subject_details=[
            SubjectDetail(subject="물리", text="역학 탐구 보고서 작성"),
        ])
    ])


def test_hallucinated_citation_flagged():
    items = [
        ScoreItem(key="탐구역량", score=4, citation="역학 탐구 보고서 작성"),  # 실재
        ScoreItem(key="리더십", score=5, citation="전교회장 활동"),            # 환각
    ]
    checked = validate_citations(items, _sg())
    by_key = {i.key: i for i in checked}
    assert by_key["탐구역량"].citation_verified is True
    assert by_key["리더십"].citation_verified is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_fit_scorer.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: Write the prompt file**

```markdown
<!-- src/classin_toolkit/intelligence/prompts/fit_score.md -->
당신은 생기부 적합도 평가자다. 구조화된 생기부와 학과 루브릭이 주어진다.
각 역량(competency)에 대해 0~5점과 근거 인용을 JSON으로만 출력(코드펜스 없이).

규칙:
- citation은 반드시 생기부에 실제 등장한 문구를 그대로 인용. 지어내지 말 것.
- 근거가 없으면 score를 0, citation을 "" 로.

스키마:
{"items": [{"key": <str>, "score": <0..5>, "citation": <str>}]}
```

- [ ] **Step 4: Write implementation**

```python
# src/classin_toolkit/intelligence/fit_scorer.py
from __future__ import annotations

import json
from dataclasses import dataclass

from ..config import AppConfig
from .claude_client import load_prompt, run_json
from .saenggibu_schema import StructuredSaenggibu


@dataclass
class ScoreItem:
    key: str
    score: int
    citation: str
    citation_verified: bool = False


def validate_citations(
    items: list[ScoreItem], sg: StructuredSaenggibu
) -> list[ScoreItem]:
    """인용이 생기부에 실제 존재하는지 대조 — 환각 차단."""
    evidence = sg.all_evidence()
    for it in items:
        it.citation_verified = bool(it.citation) and any(
            it.citation in e or e in it.citation for e in evidence
        )
    return items


def score_fit(
    cfg: AppConfig, *, sg: StructuredSaenggibu, rubric: dict
) -> list[ScoreItem]:
    system = load_prompt("fit_score")
    payload = {"saenggibu": sg.model_dump(), "rubric": rubric}
    raw = run_json(cfg, system=system, user=json.dumps(payload, ensure_ascii=False))
    rows = raw.get("items", []) if isinstance(raw, dict) else []
    items = [ScoreItem(key=r["key"], score=int(r["score"]), citation=r.get("citation", "")) for r in rows]
    return validate_citations(items, sg)
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pytest tests/test_fit_scorer.py -v`
Expected: PASS (1 passed)

- [ ] **Step 6: Commit**

```bash
git add src/classin_toolkit/intelligence/fit_scorer.py src/classin_toolkit/intelligence/prompts/fit_score.md tests/test_fit_scorer.py
git commit -m "feat: 적합도 채점기 + 인용 환각 검증"
```

---

## Task 8: 리포트 생성기 (parse 패턴) + 프롬프트

**Files:**
- Create: `src/classin_toolkit/intelligence/career_report.py`
- Create: `src/classin_toolkit/intelligence/prompts/career_report.md`
- Test: `tests/test_career_report.py`

- [ ] **Step 1: Write the failing test (parse 순수 로직 — weekly_report 패턴 복제)**

```python
# tests/test_career_report.py
from classin_toolkit.intelligence.career_report import CareerReport


def test_parse_splits_summary_and_parent_message():
    text = "## 진단 요약\n강점: 탐구역량\n\n## 학부모 카톡 문구\n안녕하세요, ..."
    r = CareerReport.parse(text)
    assert "탐구역량" in r.summary_markdown
    assert r.parent_message.startswith("안녕하세요")


def test_parse_missing_parent_section_yields_empty():
    r = CareerReport.parse("## 진단 요약\n내용만 있음")
    assert r.parent_message == ""
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_career_report.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: Write the prompt file**

```markdown
<!-- src/classin_toolkit/intelligence/prompts/career_report.md -->
당신은 진학 컨설턴트다. 채점 결과(역량별 점수·검증된 근거)를 바탕으로
학생/학부모용 리포트를 마크다운으로 작성한다.

필수 고지: 리포트 첫 줄에 "※ 본 진단은 공식 입시 기준이 아닌 학과 인재상
기반 참고용입니다." 를 넣는다.
citation_verified=false 인 근거는 인용하지 말 것.

출력 형식(정확히 이 헤더 사용):
## 진단 요약
(강점 / 공백 / 다음 액션)

## 학부모 카톡 문구
(2~3문장, 존댓말)
```

- [ ] **Step 4: Write implementation**

```python
# src/classin_toolkit/intelligence/career_report.py
from __future__ import annotations

import json
from dataclasses import dataclass

from ..config import AppConfig
from .claude_client import load_prompt, run_structured
from .fit_scorer import ScoreItem


@dataclass
class CareerReport:
    summary_markdown: str
    parent_message: str

    @classmethod
    def parse(cls, text: str) -> "CareerReport":
        parts = text.split("## 학부모 카톡 문구", 1)
        summary = parts[0].strip()
        parent = parts[1].strip() if len(parts) == 2 else ""
        return cls(summary_markdown=summary, parent_message=parent)


def build_career_report(
    cfg: AppConfig, *, major: str, items: list[ScoreItem]
) -> CareerReport:
    system = load_prompt("career_report")
    payload = {
        "major": major,
        "scores": [
            {"key": i.key, "score": i.score, "citation": i.citation,
             "citation_verified": i.citation_verified}
            for i in items
        ],
    }
    text = run_structured(
        cfg, system=system,
        user=json.dumps(payload, ensure_ascii=False, indent=2),
        model=cfg.anthropic.report_model, max_tokens=2048,
    )
    return CareerReport.parse(text)
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pytest tests/test_career_report.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: Commit**

```bash
git add src/classin_toolkit/intelligence/career_report.py src/classin_toolkit/intelligence/prompts/career_report.md tests/test_career_report.py
git commit -m "feat: 진학 리포트 생성기 (parse 패턴)"
```

---

## Task 9: Config + Notion DB6/DB7 (Layer 2)

**Files:**
- Modify: `src/classin_toolkit/config.py:28-34` (`NotionDatabases`)
- Modify: `src/classin_toolkit/storage/notion_repo.py` (PROP 상수 + 메서드)

- [ ] **Step 1: config에 career/corpus DB 추가**

`config.py`의 `NotionDatabases` (28-34행)에 두 줄 추가:

```python
class NotionDatabases(BaseModel):
    students: str
    lessons: str
    reports: str
    memos: str | None = None
    exams: str | None = None
    career: str | None = None    # DB6 진로 프로필
    corpus: str | None = None    # DB7 진학 코퍼스 (비식별)
```

- [ ] **Step 2: notion_repo.py에 PROP 상수 추가**

`PROP_EXAM_*` 블록(72행) 다음에 추가:

```python
PROP_CAREER_STUDENT = "학생"
PROP_CAREER_TARGET_MAJOR = "목표 대학/학과"
PROP_CAREER_FIT_SCORE = "최신 적합도 점수"
PROP_CAREER_CONSENT = "동의 상태"
PROP_CAREER_CONSENT_AT = "동의일시"

PROP_CORPUS_ID = "코퍼스 ID"
PROP_CORPUS_TRACK = "계열/학과 태그"
PROP_CORPUS_OUTCOME = "결과 라벨"
PROP_CORPUS_CONSENT_REF = "동의 근거"
```

- [ ] **Step 3: NotionRepo __init__에 db 인자 추가**

`NotionRepo.__init__` (75-90행)에 두 인자와 대입을 추가:

```python
    def __init__(
        self,
        token: str,
        students_db: str,
        lessons_db: str,
        reports_db: str,
        memos_db: str | None = None,
        exams_db: str | None = None,
        career_db: str | None = None,
        corpus_db: str | None = None,
    ):
        self._nc = Client(auth=token)
        self.students_db = students_db
        self.lessons_db = lessons_db
        self.reports_db = reports_db
        self.memos_db = memos_db
        self.exams_db = exams_db
        self.career_db = career_db
        self.corpus_db = corpus_db
```

- [ ] **Step 4: 진로 프로필 upsert + 코퍼스 적재 메서드 추가**

`NotionRepo` 클래스 끝에 추가 (기존 메서드들의 `_nc.pages.create` 패턴 참고):

```python
    def create_career_profile(  # 주: insert-only. 학생당 1개 보장 실upsert는 phase-2 follow-up
        self, *, student_page_id: str, target_major: str,
        fit_score: float, consent_label: str,
    ) -> str:
        """진로 프로필(DB6) 생성. 페이지 id 반환."""
        if not self.career_db:
            raise ValueError("career_db 미설정 (config.notion.databases.career)")
        props = {
            PROP_CAREER_STUDENT: {"relation": [{"id": student_page_id}]},
            PROP_CAREER_TARGET_MAJOR: {"rich_text": [{"text": {"content": target_major}}]},
            PROP_CAREER_FIT_SCORE: {"number": fit_score},
            PROP_CAREER_CONSENT: {"select": {"name": consent_label}},
            PROP_CAREER_CONSENT_AT: {"date": {"start": datetime.now(timezone.utc).isoformat()}},
        }
        page = self._nc.pages.create(
            parent={"database_id": self.career_db}, properties=props
        )
        return page["id"]

    def add_corpus_entry(
        self, *, corpus_id: str, track_tags: list[str], consent_ref: str,
    ) -> str:
        """비식별 코퍼스(DB7) 적재. 결과 라벨은 '미정'으로 시작."""
        if not self.corpus_db:
            raise ValueError("corpus_db 미설정 (config.notion.databases.corpus)")
        props = {
            PROP_CORPUS_ID: {"title": [{"text": {"content": corpus_id}}]},
            PROP_CORPUS_TRACK: {"multi_select": [{"name": t} for t in track_tags]},
            PROP_CORPUS_OUTCOME: {"select": {"name": "미정"}},
            PROP_CORPUS_CONSENT_REF: {"rich_text": [{"text": {"content": consent_ref}}]},
        }
        page = self._nc.pages.create(
            parent={"database_id": self.corpus_db}, properties=props
        )
        return page["id"]
```

- [ ] **Step 5: Verify import compiles**

Run: `python -c "from classin_toolkit.storage.notion_repo import NotionRepo, PROP_CAREER_STUDENT, PROP_CORPUS_ID; print('ok')"`
Expected: `ok`

- [ ] **Step 6: Commit**

```bash
git add src/classin_toolkit/config.py src/classin_toolkit/storage/notion_repo.py
git commit -m "feat: Notion DB6 진로프로필 + DB7 진학코퍼스 저장 메서드"
```

---

## Task 10: 파이프라인 오케스트레이션 (Layer 4)

**Files:**
- Create: `src/classin_toolkit/pipelines/career.py`
- Test: `tests/test_career_pipeline.py`

- [ ] **Step 1: Write the failing test (동의 게이트가 파이프라인 진입을 막는지 — 외부 호출 전에 차단)**

```python
# tests/test_career_pipeline.py
import pytest

from classin_toolkit.intelligence.consent import ConsentError, ConsentStatus
from classin_toolkit.pipelines.career import guard_consent


def test_pipeline_blocks_before_any_claude_call():
    # 미동의면 분석 자체를 막아야 함 (PDF/Claude 호출 도달 전)
    with pytest.raises(ConsentError):
        guard_consent(ConsentStatus.NONE)


def test_pipeline_allows_internal():
    assert guard_consent(ConsentStatus.INTERNAL) is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/test_career_pipeline.py -v`
Expected: FAIL with `ModuleNotFoundError`

- [ ] **Step 3: Write implementation**

```python
# src/classin_toolkit/pipelines/career.py
"""진학 적합도 진단 파이프라인 (Layer 4).

흐름: 동의 게이트 → 생기부 파싱 → 루브릭 → 채점 → 리포트
     → (승인 후) DB6 아카이브 + (코퍼스 동의 시) 비식별 → DB7.
패턴 D(주간 리포트): 여기서는 리포트 객체까지 생성하고,
Notion 아카이브/코퍼스 적재는 원장 승인 후 호출하도록 분리.
"""
from __future__ import annotations

from dataclasses import dataclass

from ..config import AppConfig
from ..intelligence.career_report import CareerReport, build_career_report
from ..intelligence.consent import (
    ConsentStatus, can_add_to_corpus, ensure_can_analyze,
)
from ..intelligence.deidentify import deidentify
from ..intelligence.fit_scorer import ScoreItem, score_fit
from ..intelligence.rubric_builder import build_rubric
from ..intelligence.saenggibu_parser import parse_saenggibu
from ..intelligence.saenggibu_schema import StructuredSaenggibu


def guard_consent(status: ConsentStatus) -> None:
    """파이프라인 최상단 게이트. 미동의면 즉시 raise."""
    ensure_can_analyze(status)


@dataclass
class DiagnosisResult:
    saenggibu: StructuredSaenggibu
    items: list[ScoreItem]
    report: CareerReport
    avg_score: float


def diagnose(
    cfg: AppConfig, *, pdf_bytes: bytes, target_major: str, consent: ConsentStatus,
) -> DiagnosisResult:
    guard_consent(consent)                                  # ① 게이트 (Claude 호출 전)
    sg = parse_saenggibu(cfg, pdf_bytes=pdf_bytes)          # ② 파싱
    rubric = build_rubric(cfg, major=target_major)          # ③ 루브릭
    items = score_fit(cfg, sg=sg, rubric=rubric)            # ④ 채점(인용검증 포함)
    report = build_career_report(cfg, major=target_major, items=items)  # ⑤ 리포트
    verified = [i for i in items if i.citation_verified] or items
    avg = round(sum(i.score for i in verified) / max(len(verified), 1), 2)
    return DiagnosisResult(saenggibu=sg, items=items, report=report, avg_score=avg)


def archive_corpus_if_consented(
    cfg: AppConfig, repo, *, result: DiagnosisResult, consent: ConsentStatus,
    student_names: list[str], schools: list[str], track_tags: list[str],
    corpus_id: str, consent_ref: str,
) -> str | None:
    """원장 승인 후 호출. 코퍼스활용 동의 시에만 비식별 적재."""
    if not can_add_to_corpus(consent):
        return None
    _clean = deidentify(result.saenggibu, names=student_names, schools=schools)
    # _clean(비식별 구조화 생기부)은 page children 블록으로 첨부 — repo 메서드에서 처리
    return repo.add_corpus_entry(
        corpus_id=corpus_id, track_tags=track_tags, consent_ref=consent_ref
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/test_career_pipeline.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Run full suite**

Run: `pytest -q`
Expected: 모든 테스트 PASS (기존 + 신규)

- [ ] **Step 6: Commit**

```bash
git add src/classin_toolkit/pipelines/career.py tests/test_career_pipeline.py
git commit -m "feat: 진학 적합도 진단 파이프라인 (동의 게이트 우선)"
```

---

## 미해결 (다음 작업 — 이 계획 범위 밖)

- 생기부 PDF 업로드 UI 진입점 (`agent`/`ui` 수동 라인) — 별도 작업
- `_clean` 비식별 생기부를 DB7 page children 블록으로 쓰는 상세 직렬화 — `add_corpus_entry` 확장
- 비식별화 항목 목록 정교화(교사명·고유 활동명 등 식별자 사전)
- 동의서 문구·개인정보 수집·이용 동의 법적 검토 (별도 트랙)
- 입시 시즌 결과 라벨 태깅 워크플로우 (Phase 2 진입점)

## 자체 점검 결과

- **스펙 커버리지:** 엔진 4종(Task 5~8), 동의 게이트(Task 3·10), 비식별 코퍼스(Task 2·9·10), DB6/7(Task 9), 패턴 D 분리(Task 10) 모두 매핑됨. NEIS OpenAPI 연동은 스펙 §3.4에서 "MVP 생략 가능"으로 명시 → 의도적으로 이 계획 제외.
- **플레이스홀더:** Task 6의 `_cached_rubric` 스텁은 Step 2 주석에서 "삭제하라"고 명시적으로 지시 — 최종 코드엔 미존재.
- **타입 일관성:** `StructuredSaenggibu.all_evidence()`(Task1) ↔ `validate_citations`(Task7), `ConsentStatus`(Task3) ↔ `guard_consent`(Task10), `ScoreItem`(Task7) ↔ `build_career_report`(Task8) 시그니처 일치 확인.
